---
layout: default
title: Ghetto Bunch of Drives
date: 2026-05-09 18:00:00 +0800
---

# Plan
- Use up all the spare 2.5" hard drives
- Create a cold storage solution in addition to all pre-existing

# Constraints
- Absolute minimal spend
- In addition to no new storage (previous projects) no new compute
- Couldn't use preferred Unraid because no spare x86 compute

# Infrastructure

- Raspberry Pi 4 4GB
- mBeat 7 Port Powered USB Hub (4x USB3, 3x USB2)
- 6x Various 250GB-1TB 2.5" HDDs in external drive enclosures

# Steps

## OS
- Raspberry Pi Imager
  - Rpi OS Lite (no desktop)
- Update packages: `sudo apt update && sudo apt upgrade && sudo apt autoclean && sudo apt autoremove`

## Format & Mount
- Install relevant packages: `sudo apt install mergerfs snapraid -y`
- Identify relevant disks: `lsblk -o NAME,SIZE,TYPE,FSTYPE,UUID`
- Partition: `sudo parted /dev/sdX --script mklabel gpt mkpart primary ext4 0% 100%`
- Format :`sudo mkfs.ext4 -O ^has_journal /dev/sdX1`
- Create mountpoints: `sudo mkdir -p /mnt/disk1 /mnt/disk2 /mnt/disk3 /mnt/disk4 /mnt/array`
- Gather UUIDs: `sudo blkid`
- Add to fstab: `UUID=<uuid1>  /mnt/disk1  ext4  defaults,noatime,nofail,x-systemd.device-timeout=10  0  0
UUID=<uuid2>  /mnt/disk2  ext4  defaults,noatime,nofail,x-systemd.device-timeout=10  0  0
UUID=<uuid3>  /mnt/disk3  ext4  defaults,noatime,nofail,x-systemd.device-timeout=10  0  0
UUID=<uuid4>  /mnt/disk4  ext4  defaults,noatime,nofail,x-systemd.device-timeout=10  0  0`
  - `nofail` → don’t break boot if a disk is slow
  - `x-systemd.device-timeout=10` → wait up to 10 seconds for USB spin‑up
  - `noatime` → reduces writes on HDDs
- Check they mount cleanly: `sudo mount -a`

## MergerFS
- Add mergerfs union: `/mnt/disk* /mnt/array fuse.mergerfs defaults,allow_other,use_ino,category.create=mfs,moveonenospc=true,minfreespace=10G,nofail 0 0`
- Check mounts again: `sudo mount -a && df -h`
- Reload systemd and mount everything:
  - `sudo systemctl daemon-reload`
  - `sudo mount -a`
  - `df -h`

## Snapraid
- Create config: `nano /etc/snapraid.conf` (using snapraid.conf below)
- Create log file: `sudo touch /var/log/snapraid.log && sudo chmod 666 /var/log/snapraid.log`
- Handy link: `ln -s /var/log/snapraid.log ~/snapraid.log`
- Maintenance script: `sudo nano /usr/local/bin/maintain.sh` (using maintain.sh below)
- Executable: `sudo chmod +x /usr/local/bin/maintain.sh`
- Handy link: `ln -s /usr/local/bin/maintain.sh ~/maintain.sh` (need sudo to run as non root)
- Crontab to run at boot and every six hours `sudo crontab -e`:
  ```
  @reboot /usr/local/bin/snapraid-maint
  0 */6 * * * /usr/local/bin/snapraid-maint
  ```

## Syncthing
- Per [website](https://apt.syncthing.net/)
- `https://apt.syncthing.net/`
- `sudo curl -L -o /etc/apt/keyrings/syncthing-archive-keyring.gpg https://syncthing.net/release-key.gpg`
- `echo "deb [signed-by=/etc/apt/keyrings/syncthing-archive-keyring.gpg] https://apt.syncthing.net/ syncthing stable-v2" | sudo tee /etc/apt/sources.list.d/syncthing.list`
- `sudo apt-get update && sudo apt-get install syncthing`
- Start syncthing:
  - `sudo systemctl enable syncthing@pi.service`
  - `sudo systemctl start syncthing@pi.service`
  - `systemctl status syncthing@pi.service`

## Tailscale
- `curl -fsSL https://tailscale.com/install.sh | sh`
- `sudo tailscale up`
- Login via link
- Approve device

# Files

## snapraid.conf
```                                                         
##############################################
# SnapRAID configuration for Whitefall
# Layout: 3 data disks + 1 parity disk
##############################################

# PARITY DISK
parity /mnt/disk4/snapraid.parity

# CONTENT FILES (redundant metadata)
content /mnt/disk1/snapraid.content
content /mnt/disk2/snapraid.content
content /mnt/disk3/snapraid.content
content /mnt/disk4/snapraid.content

# DATA DISKS
data disk1 /mnt/disk1
data disk2 /mnt/disk2
data disk3 /mnt/disk3

# EXCLUDES (recommended)
exclude *.tmp
exclude *.bak
exclude *.swp
exclude *.DS_Store
exclude Thumbs.db
exclude .Trash*/
exclude .recycle*/
exclude .cache*/
exclude .@__thumb*/
exclude lost+found/

# TUNING
# Autosave prevents corruption if sync is interrupted
autosave 100

# Larger blocksize = fewer I/O ops on slow USB disks
blocksize 256
```

## maintain.sh
```
#!/bin/bash

# CONFIG
SYNC_DAYS=1
SCRUB_DAYS=14
LOG=/var/log/snapraid-maint.log

echo "=== SnapRAID maintenance $(date) ===" >> $LOG

# Ensure disks are mounted
for d in /mnt/disk1 /mnt/disk2 /mnt/disk3 /mnt/disk4; do
    if ! mountpoint -q "$d"; then
        echo "Disk $d not mounted, aborting." >> $LOG
        exit 0
    fi
done

# Get last sync timestamp
LAST_SYNC=$(snapraid status | grep "Sync finished" | sed 's/.*: //')
LAST_SYNC_TS=$(date -d "$LAST_SYNC" +%s 2>/dev/null)
NOW_TS=$(date +%s)

# If no sync has ever run, force one
if [ -z "$LAST_SYNC_TS" ]; then
    echo "No previous sync found, running initial sync." >> $LOG
    snapraid sync >> $LOG 2>&1
    exit 0
fi

# Days since last sync
DIFF_SYNC=$(( (NOW_TS - LAST_SYNC_TS) / 86400 ))

if [ $DIFF_SYNC -ge $SYNC_DAYS ]; then
    echo "Last sync $DIFF_SYNC days ago, running sync." >> $LOG
    snapraid sync >> $LOG 2>&1
else
    echo "Sync not needed ($DIFF_SYNC days)." >> $LOG
fi

# Scrub check
LAST_SCRUB=$(snapraid status | grep "Scrub finished" | sed 's/.*: //')
LAST_SCRUB_TS=$(date -d "$LAST_SCRUB" +%s 2>/dev/null)

if [ -z "$LAST_SCRUB_TS" ]; then
    echo "No previous scrub found, running scrub." >> $LOG
    snapraid scrub -p 10 >> $LOG 2>&1
    exit 0
fi

DIFF_SCRUB=$(( (NOW_TS - LAST_SCRUB_TS) / 86400 ))

if [ $DIFF_SCRUB -ge $SCRUB_DAYS ]; then
    echo "Last scrub $DIFF_SCRUB days ago, running scrub." >> $LOG
    snapraid scrub -p 10 >> $LOG 2>&1
else
    echo "Scrub not needed ($DIFF_SCRUB days)." >> $LOG
fi

```
